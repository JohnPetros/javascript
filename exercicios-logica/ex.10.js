function calculateSquareDifference(a, b) {
  const squareDifference = (a - b) ** 2;
  console.log(`O quadrado da diferença entre ${a} e ${b} é ${squareDifference}`);
}

calculateSquareDifference(2, 4);
